#!/bin/sh
#
# Start the gpio driver
#

start() {
        echo "Starting gpio..."

        # load driver
        /sbin/modprobe gpio
}
stop() {
        echo "Stopping gpio..."

	/sbin/modprobe -r gpio
}
restart() {
        stop
        start
}

case "$1" in
  start)
        start
        ;;
  stop)
        stop
        ;;
  restart)
        restart
        ;;
  *)
        echo $"Usage: $0 {start|stop|restart}"
        exit 1
esac

exit $?

